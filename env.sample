# Copy this file to .env and fill in your values

# Required: OpenAI API key for image generation
OPENAI_API_KEY=sk-your-key-here

# Optional: Image generation settings
# MODEL_IMAGE=dall-e-3          # Use "dall-e-3" or "dall-e-2"
# MODEL_VISION=gpt-4o           # Vision model for analyzing child's appearance
# IMAGE_SIZE=1024x1024          # Image size (1024x1024, 1792x1024, or 1024x1792 for DALL-E 3)
# IMAGE_QUALITY=standard        # Quality: "standard" or "hd" (DALL-E 3 only)

# Optional: Corporate SSL/proxy support (for internet filters, corporate proxies, etc.)
# REQUESTS_CA_BUNDLE=C:\certs\corp-root.pem    # Path to your organization's CA certificate bundle
# SSL_CERT_FILE=C:\certs\corp-root.pem         # Alternative: SSL certificate file
# HTTPS_PROXY=http://user:pass@proxy.host:8080 # Proxy server (if required)
# OPENAI_HTTP_TIMEOUT=120                      # Request timeout in seconds

# IMPORTANT: If you have an internet filter/proxy that intercepts SSL:
# Option 1 (RECOMMENDED): Export the filter's certificate and set REQUESTS_CA_BUNDLE
# Option 2 (NOT SECURE): Set OPENAI_DISABLE_SSL_VERIFY=1 (use only if Option 1 doesn't work)
# OPENAI_DISABLE_SSL_VERIFY=0                  # Set to "1" to disable SSL verification (NOT RECOMMENDED)

# Optional: Debug/Testing
# FORCE_PLACEHOLDER=0           # Set to "1" to skip OpenAI and use placeholder images
# SKIP_VISION_API=0             # Set to "1" to skip child photo analysis (faster, less accurate)

# Optional: override port or other app settings here
# FLASK_RUN_PORT=5000

# Database Configuration (XAMPP MySQL)
# Configure for local XAMPP MySQL database
# 
# IMPORTANT: Make sure XAMPP MySQL is running before starting the app
# Start MySQL from XAMPP Control Panel if needed
#
# Default XAMPP MySQL settings:
DB_HOST=localhost
DB_PORT=3306
DB_NAME=mystory
DB_USER=root
DB_PASSWORD=
#
# If you set a password for MySQL root user in XAMPP, uncomment and set it:
# DB_PASSWORD=your_mysql_password
#
# Note: Create the database first using phpMyAdmin or MySQL command line:
# CREATE DATABASE mystory CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# OAuth Configuration (for user login)
# Get these from your OAuth provider developer consoles
# SECRET_KEY=your-secret-key-here  # REQUIRED: Random string for session encryption (required for OAuth)
#                                  # Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
#                                  # IMPORTANT: Must be fixed value, not random on each restart!

# Google OAuth (https://console.cloud.google.com/apis/credentials)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_REDIRECT_URI=http://localhost:5000/auth/google/callback  # Optional: override redirect URI
# OAUTH_BASE_URL=http://localhost:5000  # Base URL for OAuth redirects (default: http://localhost:5000)

# Facebook OAuth (https://developers.facebook.com/apps/)
# FACEBOOK_CLIENT_ID=your-facebook-app-id
# FACEBOOK_CLIENT_SECRET=your-facebook-app-secret

# Apple Sign In (https://developer.apple.com/account/resources/identifiers/list)
# NOTE: Apple Sign In requires a paid Apple Developer account ($99/year)
# APPLE_CLIENT_ID=com.yourname.mystory.web  # Your Services ID
# APPLE_CLIENT_SECRET=eyJraWQiOi...  # JWT token (generate with generate_apple_secret.py)
# APPLE_REDIRECT_URI=http://localhost:5000/auth/apple/callback  # Optional: override redirect URI
# 
# Optional: For generating client secret dynamically (advanced)
# APPLE_TEAM_ID=ABC123DEF4  # Your Team ID from Apple Developer account
# APPLE_KEY_ID=XYZ789  # Your Key ID
# APPLE_PRIVATE_KEY_PATH=path/to/AuthKey_KEYID.p8  # Path to your .p8 private key file

# Email Configuration (for email verification and password reset)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=your-email@gmail.com
# APP_URL=http://localhost:5000  # Your app URL for email links

# Storage Configuration (for user book storage)
# STORAGE_TYPE=local  # Options: local, s3, gcs, cloudinary
#
# For local storage (default):
# Books are stored in: outputs/{user_id}/{user_id}_{timestamp}_{story_id}.pdf
# Thumbnails are stored in: static/thumbnails/{user_id}/
#
# For AWS S3:
# STORAGE_TYPE=s3
# AWS_S3_BUCKET=your-bucket-name
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
#
# For Google Cloud Storage:
# STORAGE_TYPE=gcs
# GCS_BUCKET=your-bucket-name
# (Set GOOGLE_APPLICATION_CREDENTIALS environment variable to your service account JSON)
#
# For Cloudinary:
# STORAGE_TYPE=cloudinary
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret

